require 'spec_helper'

describe PizzaOvenPlanner do
  it 'has a version number' do
    expect(PizzaOvenPlanner::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
